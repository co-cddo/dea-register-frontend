<h1>Search results</h1>
<p>Searching for: "<%= params[:q] %>"</p>

<ul class="govuk-list">
  <% @search.map(&:searchable).each do |object| %>
    <li>
      <%= link_to "#{object.type} : #{object.name}", object %>
    </li>
  <% end %>
</ul>

<%= govuk_pagination(pagy: @pagy) %>
