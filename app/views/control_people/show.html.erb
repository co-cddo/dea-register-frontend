<% content_for :page_title, "Controller - #{@control_person.name}" %>

<h1 class="govuk-heading-l"><%= @control_person.name %></h1>

<p class="govuk-body"><%= @control_person.fields['name'] %><p>

<%= render 'shared/associations', associations: { agreements: @control_person.agreements, powers: @control_person.powers } %>
